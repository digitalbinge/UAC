<% @cart_items.each do |item| %>
	<%= image_tag item.product.image %> <br>
	<%= item.product.price %> <br>
	<%= item.quantity %> <br>
	<%= item.size %> <br>
	<%= link_to 'Edit', edit_product_path(item.product_id), class: 'btn btn-secondary' %> <br>
<% end %>

